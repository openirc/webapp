
import { loggedIn } from 'frontend/helpers/logged-in';
import { module, test } from 'qunit';

module('Unit | Helper | logged in');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = loggedIn([42]);
  assert.ok(result);
});

